return {
  {
    "neoclide/coc.nvim",
    branch = "release", -- Рекомендуется использовать ветку release
    build = "npm install", -- Автоматическая установка зависимостей при установке плагина
    config = function()
      -- Здесь обычно настраивают привязки клавиш (Keybindings)
    end,
  },
}
